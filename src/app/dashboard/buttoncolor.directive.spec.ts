import { ButtoncolorDirective } from './buttoncolor.directive';

describe('ButtoncolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtoncolorDirective();
    expect(directive).toBeTruthy();
  });
});
